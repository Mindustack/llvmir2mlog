package org.mindustack.llvmir2mlog.compiler.middleend.rewrite;

import org.mindustack.llvmir2mlog.compiler.middleend.llvmir.hierarchy.IRModule;
import org.mindustack.llvmir2mlog.compiler.share.pass.IRModulePass;

public class MiddleEndRewriter implements IRModulePass {
    @Override
    public void runOnModule(IRModule module) {

    }
}
